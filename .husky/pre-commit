branch="$(git rev-parse --abbrev-ref HEAD)"

if [ "$branch" = "main" ]; then
  echo "Direct commits to 'main' are blocked. Create a feature branch:"
  echo "    git switch -c your-branch-name"
  echo "  To bypass (not recommended), re-run with --no-verify."
  exit 1
fi
