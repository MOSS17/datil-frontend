# Design Tokens — superseded

**This file has been replaced by [`figma-tokens.md`](./figma-tokens.md).**

`figma-tokens.md` is the canonical source. It includes:
- Full Figma variable → Tailwind class mapping (bg, text, border, icon, spacing, type, radius)
- Transformation rules for the MCP's variable path syntax
- Failure-mode checklist with diagnostic commands

Don't edit or read this file — go to `figma-tokens.md`. If you need a token that's missing, add it there and to `tailwind.config.js` in the same change.
